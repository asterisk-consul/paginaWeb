---
import Target from "../icons/Target.astro";
import Users from "../icons/Users.astro";
import Zap from "../icons/Zap.astro";
import TeamSection from "./TeamSection.astro"; // tu versión convertida antes

// Elementos a animar
const fadeItems = [
  { selector: "#title", delay: 0 },
  { selector: "#subtitle", delay: 200 },
];
---

<div class="animate-fade-in">
  <!-- HERO -->
  <section class="py-20 md:py-28 bg-dark-card">
    <div class="container mx-auto px-6 text-center md:text-left">
      <h1
        id="title"
        class="font-family-tight font-regular text-6xl md:text-8xl tracking-tighter text-balance mb-4 opacity-0 translate-y-12"
      >
        Nuestra Misión:
        <span
          class="bg-gradient-to-r from-red-500 to-orange-500 bg-clip-text text-transparent italic pr-8"
        >
          Transformar Procesos
        </span>
      </h1>

      <p
        id="subtitle"
        class="text-lg md:text-xl text-center md:text-left text-foreground/80 max-w-3xl opacity-0 translate-y-12"
      >
        Ayudamos a las empresas a digitalizar, administrar y optimizar sus
        operaciones para tomar decisiones más inteligentes y eficientes.
      </p>
    </div>
  </section>

  <!-- CARDS -->
  <section class="py-20">
    <div class="container mx-auto px-6 grid md:grid-cols-3 gap-10 text-center">
      <div
        class="border border-dark-border p-8 rounded-xl motion-card opacity-0 translate-y-12"
      >
        <Target class="w-12 h-12 text-brand-primary mx-auto mb-4" />
        <h3 class="text-2xl font-bold text-foreground mb-2">Visión</h3>
        <p class="text-foreground/80">
          Ser el socio estratégico líder en la transformación digital de
          procesos empresariales en Latinoamérica.
        </p>
      </div>

      <div
        class="border border-dark-border p-8 rounded-xl bg-dark-card motion-card opacity-0 translate-y-12"
      >
        <Users class="w-12 h-12 text-brand-primary mx-auto mb-4" />
        <h3 class="text-2xl font-bold text-foreground mb-2">Filosofía</h3>
        <p class="text-foreground/80">
          Creemos en la colaboración estrecha, la transparencia total y la
          entrega de resultados medibles.
        </p>
      </div>

      <div
        class="border border-dark-border p-8 rounded-xl motion-card opacity-0 translate-y-12"
      >
        <Zap class="w-12 h-12 text-brand-primary mx-auto mb-4" />
        <h3 class="text-2xl font-bold text-foreground mb-2">Enfoque</h3>
        <p class="text-foreground/80">
          Combinamos tecnología con un profundo entendimiento del negocio para
          crear soluciones con impacto real.
        </p>
      </div>
    </div>
  </section>

  <TeamSection />
</div>

<script>
  import { inView, animate } from "@motionone/dom";

  // Animación del título y subtítulo
  const fadeItems = [
    { selector: "#title", delay: 0 },
    { selector: "#subtitle", delay: 200 },
  ];

  fadeItems.forEach(({ selector, delay }) => {
    inView(selector, ({ target }) => {
      animate(
        target,
        { opacity: 1, transform: "translateY(0)" },
        { duration: 0.6, delay: delay / 1000, easing: "ease-out" }
      );
    });
  });

  // Animación de las tarjetas
  inView(".motion-card", ({ target }) => {
    animate(
      target,
      { opacity: 1, transform: "translateY(0)" },
      { duration: 0.6, easing: "ease-out" }
    );
  });
</script>
